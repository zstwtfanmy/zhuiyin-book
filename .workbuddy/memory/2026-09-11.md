# 2026-09-11 工作日志

## 诊断：前三章为什么没有吸引力（框架 vs 模型 归因）

作者反馈：前三章没吸引力／主角出场啥都没交代／内容很干／对话没新意／主角很木。要求参考《隐杀》对照，判定是框架问题还是模型问题。

**结论：框架为主因（约 70%），执行层工艺 20%，评估机制 10%。不是模型没写好，是模型把框架要求的写对了。**

### 核心证据（正文 = 样张，这是判定框架为主因的关键反证）
- ch001 正文与 `作者性/语体样张.md` **几乎逐句对应**（扩写非重写）。执行层无发挥失误。

### 四个症状的溯源（均为框架白纸黑字的要求）
- **木** ← `执行层一页纸` 禁令 #2「不许写心理活动」+ `语言纪律` §0「贴身但不进入，不可写判断与命名」。实测 ch001 沉默类叙述 10 处。
- **对话没新意** ← `语言纪律` §4 对话签名字数上限（主角≤15/小满≤10/老爸≤8）+"几乎不问问题"+"鼓励俗套、故意用最没信息量的话"。实测对话中位 **4 字**，无问句。
- **干** ← 细纲「密 200/疏 80」字数配额被执行层读成**换行频率**。实测均段 15-21 字，≤12 字段占 46-53%。★`待审台账` 已抓到此项（overcompressed 62%≤15 字）但**判为"有意节奏"准备豁免**。
- **没交代/没吸引力** ← `细纲_第001章`「本章唯一解密位：无」；三章零解密；ch001「不给大爽只给微打脸+认证」；隐杀式 7 次暴起第 1 次在 ch007。（此为用户 2026-09-11 首次提出）

### 对标《隐杀》：顺序用反了
- 「扮猪吃老虎」用在**读者还没见过老虎**的时候 → 无落差。
- 「温暴比」的"温"被实现成**环境质感**（年代物件密度）而非**人物魅力**。
- 沉默性质错：隐杀的"不解释"是性格（能说而不肯）；本书是纪律（不能说）→ 魅力变失语。
- **卷纲 vs 执行层直接打架**：卷纲承诺「轻快喜剧／老灵魂＋嬉皮笑脸」，执行层规定「主角不搞笑」。`待审台账` 已记「气句'玩世不恭'前三章未落地」。

### 执行层的账（20%，均不在"好看"上）
AI 指纹（micro-action-tic ch001 16／ch002 22／ch003 20，逐章递增）、逻辑错（ch003 老爸问话与"一直没说话"矛盾、酒时序倒错）、**功能性失效（ch003 三问两问空转、ch002 接梗模板复用 ch001）**、照抄细纲散文句。

### 待作者确认（★重要）
- `待审台账` 全部条目标注 **`review(solo)`** → 前三章疑为 **solo 降级**（主会话写）。
- `.codex/agents/guyin-beat-writer.toml` 的 `model = "deepseek-chat"` **是注释掉的** → **低模型执行层可能从未启用**。若成立，"模型不行"基本可排除。

### 评估机制失效（危害最大）
- 十轮评估 9.6 是**纸面分**；第十轮预判的三风险（憋死／装乖不成立／文艺腔）**全中**，但无机制回查。
- 30+ 条章检报警**零条**涉及吸引力；`追踪/读者信号.md` 至今**空表**；状态门"未终态阻塞新章"已失效（正文已到 ch008）。

### 交付
- 新建 `诊断_前三章失吸引力归因.md`（含 A/B/C/D 四档改法 + 三个待拍板项）。
- 改法摘要：A 开"三不写"前三章局部豁免＋碎段合并（≤12 字段 46%→25%，均段 21→32-40）＋景物挂视线；B 刘虎打脸提前／给主角当下目标；C 补"人物魅力场"，以卷纲调性为准；D 加"吸引力门"＋样本分＋追读风险维度。

### 状态
- **未动正文一字**（作者仅要求分析）。大修动刀前须先拷 `正文/_archive/`，落盘后 tracking-commit + 章检链同跑。

---

## 追加（同日）：框架层问题清单（交外部模型处理）

作者要求：不只改文档，要**梳理框架层面的问题**，形成可派活的文档交其他模型处理。

### 交付
- 新建 `框架问题清单_交外部处理.md`（项目根目录）。

### 清单结构（四类 + 工单）
- **F 类（8 条）**：框架主动做出的设计选择，在开篇场景必然产出坏结果（F-01 三不写→失语／F-02 对话三重压死／F-03 密疏用字数定义节奏／F-04 四件全空／F-05 全是远期期票／F-06 卷纲与执行层对立／F-07 扮猪吃老虎时序错／F-08 减负都站在 200 章视角而非"刚点开第一章的读者"视角）。
- **S 类（9 条）**：框架自身机制缺陷（S-01 只加规则不验证／S-02 无读者回路／S-03 检测无吸引力维度／S-04 状态门失效／S-05 执行层从未验证／S-06 无纪律冲突检测／S-07 关键术语无定义表／S-08 样张成天花板且无二次校验／S-09 私人切口挂 11 轮）。
- **H 类（9 条）**：历史遗留与误关（H-01 overcompressed 误判豁免、H-05 N1"变强→得不到"以"接受"关闭但无对冲等）。
- **C 类（8 条）**：**纪律冲突矩阵**（必须先解）。含 C-01 卷纲喜剧 vs 一页纸不搞笑；C-04 望诊慢镜要写脑内证候 vs 三不写禁心理；C-05 破折号特批被去 AI 味流程反推；C-08"写时全忘"vs"落盘必查"。
- **工单**：批次 A（设计决策，需作者拍板）／批次 B（机制修复，可独立执行）／批次 C（清账）。

### 新增待办（已写入清单，尚未执行）
- 新建 `大纲/术语表.md`、`大纲/开篇留存标准.md`、`作者性/纪律冲突台账.md`
- 章检链加 `reader-risk` 与 `rule-conflict` 两个维度
- `细纲分级与模板.md` §二 约束 3 重写：「密/疏」改为三级操作描述 + 单章 ≤15 字段 ≤25%
- 二次修订 `MEMORY.md` 中 overcompressed 的豁免结论
