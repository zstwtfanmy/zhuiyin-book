# 2026-09-18 工作日志

## ch001 框架检验诊断（用户三问落地）

- 产出 `大纲/框架诊断_第001章_20260918.md`（决策输入，只诊断不动刀）。
- 三问结论：
  1. **开头没变＝执行层保守，非框架失效**。细纲第四组把旧稿开头锁进「功能性原话授权区」，执行照抄；框架 21 门/追踪/指纹全跑通＝结构层生效，但框架是护栏非发电机，不产出吸引力。**不需要重新开书**，重做开头走 revision 通道。
  2. **破折号"——"两层问题**：①新稿 L13/L23 两处"——"成行（旧稿 0 处）替代文字过渡；②更严重——删掉了"往前倒三十八年"**世次时间锚句**（破 E 节硬约定，读者失去"重生回童年"的时间定位）。这是执行层"减负"误删，已记档。
  3. **对比隐杀：没有。** 钩子=情绪钩非行动钩；第一金手指（望诊）被隐化、不兑现情绪回报（憋不爽）；主角全程被动零发问；情绪底色"丧"非"燃"。根子＝定位错位（"前爽后细腻" vs 落地"细腻但不爽"），非能力问题。
- 未处理问题清单分四类：A 吸引力层(A1 开头未重做/A2 爽点隐化/A3 主角被动/A4 底色丧)、B 格式契约层(B1 破折号+世次锚丢/B2 ASCII 引号)、C 框架短板(C1 delta 3072 紧/C2 伏笔编号口径/C3 并发 Edit 丢更新)、D 不需重开书但重做走 revision。
- 下一步优先级：先定"第一章要爽还是要隐"→ 若爽则重做开头+兑现爽点+给主角主动 → 修 B1/B2（无论定位都要修）→ C1/C2/C3 反馈上游。

## 附：待用户决策的关键分叉
- 第一章"爽 vs 隐"是 A1–A4 共同上游，未定则重写多少次都是"细腻但不爽"。
- ch001 已 publish（last=1），改开头必须走 `tracking-commit revision` 或 `guyin-tracking-reset` 清盘，不能直接改文件。

## v2 修正（用户要求聚焦框架问题 + 落地细节，读源码后重写）
- **纠正 v1 误判**：v1 说"框架无标点检查门"是错的。读了 `guyin-normalize-punctuation.js` 源码：框架有门，能识别"——"（正则 `——|—|--+`），且单独成行的"——"会被 `choosePauseReplacement` 替换为空。
- **"——"漏网三层根因**（落地到代码行）：①em-dash severity=advisory（L302），`---`=blocking（L179）——定级过低；②门挂 SKILL.md L44（第 4 步拼接），**不在 tracking-commit.py prepared 全检**（全检只跑 pending L2049 + repetition L2472/2488）——无后置阻断；③执行层第 4 步漏跑（我章检链跑了 17 门唯独漏了它）。
- **框架统一根因**（架构师判断）：框架=后验正确性检查器+前置自动改写器，三处脱节——①只管"写对没有"不管"该留的留了没"（误删锚句零报）②前置自动修正与后置阻断脱节（前置漏跑后置不拦）③保护定稿默认开、无开关无边界（吞掉"推翻重做"授权，开头锁死）。
- **修复清单**：框架 F1 em-dash→hard / F2 纳入全检 / F3 授权区加边界 / F4 加重写开关 / F5 一次性时间锚登记位 / F6 delta 分档 / F7 伏笔编号口径 / F8 并发 Edit 串行；本项目 P1 补回"往前倒三十八年" / P2 清"——" / P3 补跑 normalize-punctuation / P4 开头重做走 revision。
