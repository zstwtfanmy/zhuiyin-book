# 框架终版整改书（第三版 · 架构改造版）

> 日期：2026-09-16。性质：**唯一权威执行文档**，覆盖第一版（双引擎）与第二版（验真版）。
> 一句话：**框架的病根不是"缺机制"，也不是"机制没被执行"，而是「为多模型协作设计的架构，跑在了单模型环境上」。三端分离里的"执行层"（低模型填空写手）从未真正运行，导致"创作权"——产生趣味的唯一来源——被一个「为低模型设计、但低模型根本不存在」的降级约束锁死了。**
>
> 本版回答两件事：**根因到底是什么**（§1）、**怎么改**（§2 改造 + §3 决策 + §4 验收）。

---

## 0. 你的判断，被证实了

> 「内置的模型没有用到，之前的创意思路是好的，但没有发挥出来。」

这句话精确命中了一个此前所有诊断（包括本框架自己）都绕开的事实：

**框架的"三端分离"是"三模型分离"——编排层（强模型）、执行层（低模型）、检查层（低温模型）。而执行层这个"低模型"从头到尾是占位符，从未真跑起来。**

| 位置 | 现状 | 说明 |
|---|---|---|
| skill `agents/guyin-beat-writer.md` 第 4 行 | 「不预置 model：继承主会话模型即等同 solo」+ `tools: ()` | 执行层无独立模型、无工具 |
| 项目 `.claude/agents/guyin-beat-writer.md` | `model: deepseek-flash（改成你的低模型…）` | 模板占位，未生效 |
| 项目 `.codex/agents/guyin-beat-writer.toml` | `# model = "deepseek-chat"` | 被注释掉 |
| `SKILL.md` 第 62 行 | 「无 subagent 环境……降级 solo：主会话扮演执行层」 | 实际运行路径 = 主会话直写 |

**结论**：所谓"执行层"，实际就是主会话自己。但主会话写正文时，被 `beat-writer` 里那套"填空式写手"的语义约束着——「只写卡上指定的片段、不写前情不下文、输出只有正文不解释不点评、必须发生的每项都写、禁止的每条不碰」。

而这套约束，**本是为"低模型不可控"设计的锁**。低模型能力弱、易跑偏、易注水，所以要锁死。**现在锁没锁住低模型（低模型根本不存在），锁死的是唯一真正在写正文的最强模型。**

这就是"创意思路好、但没发挥出来"的精确机制：
- 创意（意外、涌现、角色做细纲没规定的事）只能来自"有创作权的作者"；
- 框架把"作者"这个角色定义成了"填空式写手"（零创作权）；
- 而"填空式写手"是为低模型设计的降级——**低模型没了，降级却留下了，压在了最强模型头上。**

---

## 1. 根因（架构层精确诊断）

**一个元根因，三个投影：**

> **为"多模型协作"设计的架构，跑在了"单模型"环境上。架构没有随环境降级，反而把"多模型"的约束，反向施加给了唯一的模型。**

三个投影：

1. **执行层创作权被"填空"锁死**——`beat-writer` 定位"填空式写手"，涌现=违规的语义，是为低模型防跑偏设计的。单模型环境下，这个锁锁的是最强模型的创意。
2. **"分离"的价值被"模型"的形态绑架**——分离本来的价值是"写作时不想验收、验收时不想写作"（心智隔离）。但实现成"三个模型"后，执行层模型一缺失，分离就名存实亡，主模型一个人既要被"填空"约束、又要自我验收，两头都做不好。
3. **创意通道虽在、但无人执行**——涌现转正（SKILL.md 第49行）、偏差可认（细纲协议第78-84行）其实都写好了，但因为"执行层=填空"这个定义压着，主模型写正文时根本不会去"涌现"，通道成了摆设。

**关键认知**：这不是"执行层该不该有创作权"的问题，而是"**在多模型前提下，执行层被设计成无创作权（填空）是合理的；但前提（多模型）不成立时，这个设计必须连同前提一起废除，而不是把无创作权的约束硬套给唯一的模型。**"

---

## 2. 改造方案：三模型分离 → 单模型三角色分离

> 铁律：**不再依赖任何外部低模型。** 三个角色由同一个主模型扮演，靠"角色模式提示词 + 流程切换"隔离，不靠切换模型。脚本和自检卡本就是零温机械判定，单模型环境完全成立，**不改**。

### 核心动作：把"执行层 = 低模型填空写手"重构成"作者角色 = 有创作权的主模型"

| # | 落点 | 改法 |
|---|---|---|
| R1 | `agents/guyin-beat-writer.md` | **重写定位**：删除"填空式写手"定义，改为「**你是这一拍（beat）的作者**」。任务卡分三块：**锁死契约**（必须发生的因果/情绪目标/禁止项，逐条兑现）＋**开放空间**（人物可以有计划外的动作、停顿、台词——做一件卡上没写但符合性格的事）＋**禁令**（底线）。新增两条规则：① 开放空间里有真实创作自由，涌现是资产不是违规；② 输出末尾附一行「本拍涌现：xxx（无则写无）」供编排层收编。**删除 model/tools 占位假设，明写"本角色由主会话扮演，非独立低模型"。** |
| R2 | `SKILL.md` 第 62 行「执行层调用协议」 | 把"solo 降级"从**例外**改为**唯一路径**：删除"低模型三端写法"的假设，明写"执行层与检查层均由主会话分饰角色；角色切换靠本文件提示词约束，不靠模型切换"。 |
| R3 | 项目 `.claude/`、`.codex/`、`.opencode/` 三端部署件 | 删除 model 占位与"低模型"注释，统一指向"单模型三角色"协议（或直接指向 skill 的 beat-writer 新定义）。 |
| R4 | `references/细纲协议.md` 字段模板 | 新增「**留白声明**」字段：`{锁死区：…（因果/钩子/锚句/禁止提前释放）；开放区：…（允许现场生长，如"老爸的反应方式"）}`——把"哪里有创作空间"从隐性变显性。 |
| R5 | `SKILL.md` 写章循环第 8 步 | 「灵感登记」正名「**涌现转正**」：正文涌现的、细纲外的好东西，登 `追踪/灵感台账.md`，下批细纲决定转正，不被拦截。（措辞升级，机制已存在） |

### 保持不变的部分（这些是对的，别动）

- **检查层**：脚本 + 自检卡，零温机械判定，单模型环境完全成立。
- **正确性闸门**：合规/不崩/不泄密/字数/锚句落地——保底，与趣味性正交。
- **自检卡 Q9-11**（主角主动/对话增量/记忆点）+ **4a 两段式初读** + **黄金三章四留存/即兑钩子**——读者信号机制，已就位，随 R1 解禁创作权后自然激活。
- **趣味性验收靠读者信号，不靠脚本、不靠"趣味评分器"**（机械判不了趣味）。

### 改造后的心智模型

```
原：编排层(强模型) ──拆beat──▶ 执行层(低模型,填空) ──▶ 检查层(低温模型)
                                    ↑ model 占位，从未运行

新：主模型 ──①编排角色(全局,理性)──▶ 拆beat/验收
         ──②作者角色(高温,有创作权,能涌现)──▶ 写正文
         ──③检查角色(零温,机械)──▶ 脚本+自检卡
    三角色 = 同一主模型，靠提示词+流程切换，不靠模型切换
```

---

## 3. 我替你做的决策（8 项，均已裁决）

| # | 决策点 | 裁决 | 依据 |
|---|---|---|---|
| D1 | 执行层用低模型还是主模型角色 | **主模型角色** | 低模型 model 从未配置成功；单模型架构更稳、可复现、不依赖外部部署 |
| D2 | "填空式写手"定义删还是留 | **删** | 它的存在理由是"低模型可控"，理由已不存在 |
| D3 | 创作权（涌现）给不给 | **给** | 产生趣味的唯一来源；不给=继续"没意思" |
| D4 | 正确性闸门保留吗 | **保留** | 保底，与趣味性正交，不冲突 |
| D5 | 趣味性怎么验收 | **读者信号**（自检卡 Q9-11 + 4a 初读 + 真人试读） | 机械判不了趣味，脚本硬判只会逼出装饰 |
| D6 | 任务卡还用不用 | **用，降级为"专注脚手架"** | 保留"聚焦当前 beat、防写飘"的价值，去掉"锁死" |
| D7 | "三端分离"还要不要 | **要，改成"三角色分离"** | "写作/验收/编排三种心智分开"是对的；错的是用"三模型"实现 |
| D8 | 根治"换书还是这效果"的动作 | **改造后拿旧 ch001 重写一章验证** | 用真实样本证明"解禁创作权"后真的不木了 |

---

## 4. 验收标准

1. **架构自洽**：改造后，框架内不再出现"低模型/填空式写手/三端部署"的残留假设；执行层定义与运行路径一致（都是"主模型分饰作者角色"）。
2. **负面样本回归**：拿旧 ch001 重写一章，走新架构——必须出现**细纲外的涌现**（计划外的好东西）且被「涌现转正」收编而非拦截。
3. **铁律不破**：新增机制只保证"有趣的空间"存在，不新增"趣味评分器"；趣味性判断归读者信号，不归脚本。
4. **样本 > 论证**：改造是否有效，以"重写后一章 vs 旧 ch001"的对照 + 试读数据为准，不许纸面论证。

---

## 5. 一句话（第三版 · 终版）

**框架的病根，是"为多模型设计的锁，锁在了单模型身上"。解药不是换引擎、不是加机制，而是承认"只有一个模型"这个事实，删掉"填空式写手"，把创作权还给唯一真正写正文的那个人——然后让角色切换（编排/作者/检查）靠流程而非靠模型实现。**

**分工铁律（修正版）：编排角色保结构，作者角色保涌现，检查角色保正确，读者信号保趣味。** 前三个由同一个主模型分饰，最后一个由真实读者提供——不再有任何一步依赖"不存在的低模型"。
