# 追影 — 项目重部署 / 状态层重置报告

日期：2026-09-17
技能版本：guyin-setup 0.8.0 / guyin-write 0.14.0
部署标记：`.guyin-deployed` → guyin_version 0.14.0，form = long+short

---

## 一、技能与部署件（已完成）

- `.claude/` `.codex/` `.opencode/` 六个 agent 文件 + `guyin-hook.js` + `guyin.md` → 已刷新到 0.14.0；`.claude/settings.json` 合并完成
- 旧件备份：`.guyin/upgrade-backup/2026-09-17/`（含 MANIFEST + 回滚说明）
- **model 未回填**（按指示清掉）→ 执行层当前**等同 solo**：写作走「执行层调用协议」solo 三条（允许直写卡可选 / 笔法只取规格行 / 逐章留痕）
- 项目独有文件（`追踪/意象台账.md`、`时间线交叉表.md`、`段落指纹库.json`、`追踪/角色状态/`、`追踪/_旧追踪存档/`）已保留

## 二、按指示：不恢复，改为重新部署生成

| 项 | 处置 |
|---|---|
| `追踪/复沓锚句.md` | 旧内容移入存档 → **按 0.14.0 模板重新生成**（1480 字节，与模板逐字节一致） |
| 旧内容存档 | `追踪/_旧追踪存档/复沓锚句_重置前v0.11.md`（5633 字节原样） |
| `正文/第001章_那碗面.md`、`大纲/细纲_第001章.md`、`追踪/逐章记录/第001章.md` | ⛔ **未做任何恢复**，保持删除状态 |

> 另：`正文/` 下第002、003 章的活跃稿也不在工作区（只剩 `_archive/` 的 v1/v3）；这两个文件此前未被 git 跟踪，故不出现在 status 里。

## 三、状态层重置（A 方案，已执行并校验通过）

**重置前症状**：`guyin-tracking-commit.py check` 报 `ERROR: chapter delta 1 is missing`——
`_tracking-state.json` 写着 `last_committed_chapter: 1 / state_revision: 22`，但它的 delta 源（`追踪/逐章记录/第001章.md`）已被删除，任何事务校验都不放行。

**执行**（用脚本自己的 `init` 通道，不手写视图——Markdown 全部由 JSON 确定性派生）：

1. 整目录备份 `追踪/` → `.guyin/state-reset-backup/2026-09-17/追踪-pre-reset/`（49 个文件）
2. `_tracking-state.json` 移出项目 → 同目录 `_tracking-state.json.pre-reset`
3. 以 `.guyin/state-reset-backup/2026-09-17/init-input-ch000.json` 为输入跑 `init` → 脚本重建 state 并**重渲染全部 10 份派生视图**
4. 校验：`init` exit 0、`check` exit 0（`{"last_committed_chapter": 0, "state_revision": 0}`）

**结果**：`schema_version 7` / `last_committed_chapter 0` / `imported_through_chapter 0` / `state_revision 0`；
characters、foreshadow、verdicts、evidence、geo、scenes、chapter_summaries 全空。
脚本自动清掉了 `追踪/角色状态/` 下 5 张 ch001 派生卡片（李国强/李婶/李秀兰/沈亦舟/沈建国）。

### 捞回的设计不变式（`上下文.md` 已渲染，逐字可查）

**长期约束 6 条**（顶格用满，脚本上限就是 6）：
1. C101｜世次口径 —— 老爸的病不许推翻；「四十七岁那年」＝第二世（1975-2022），ch183 兑现「没答上的那句」不解释年龄；**本世卷五＝2022-2030、ch194 老爸走＝2029、不许推到 2038**
2. 倒叙钩子（前 260 字）之后全书不再闪回；重生/穿越/前世/系统 四词零出现
3. 第三人称限知 POV＝沈亦舟；望诊的话必须土（禁中医术语）
4. 章尾末句必须是名词或动作
5. 复沓锚句每章落一次：「手放在膝盖上」每章上限一次、「他把杯把手转了一下，朝外」（ch194 回收）；望诊不复述锚句本体，用「搁在膝盖上的那只手」变体
6. 「看手」四连、「他是要抽」、「医生也是这么说的」归属 ch001，后续只引用不重述

**下一章承诺 5 条**（ch001 的硬承诺）——含 **F001/F002/F003 的规划登记**：
> ⚠️ 脚本 schema 硬规定：`last_chapter=0` 时**禁止登记任何 foreshadow**（`a chapter-0 project cannot have planted foreshadow facts`）。
> 伏笔的点是「ch001 埋」，所以 F001–F003 只能落在 next_chapter_commitments，由新 ch001 的提交正式登记为 F001/F002/F003。
> 已写入：F001「那杯水」→第4章回收 / F002「老爸的病」→第15章回收 / F003「那句没听完的话」→第183章回收。

**连贯性风险 5 条**：水/酒两物证不可混 · ch001 零主动发问但 ch004 前须兑现一次主动索取 · 叙述层关系称谓禁全名复读 · 卷一须埋一次世次物证 · 旧稿（含 `_archive`）不作基线。

## 四、状态层之外仍带旧稿痕迹的 4 处（未动，等你一句话）

| 文件 | 现状 | 我的建议 |
|---|---|---|
| `追踪/意象台账.md`（8.9K） | 两张表全是旧稿自动沉淀（域/次数/最近章 第1–8章；复读短语「看了一眼」等） | **可清空两表**——纯 `--commit` 自动来源，无人工登记，重写时自动重沉淀 |
| `追踪/段落指纹库.json`（339K） | 全部是已删章节的段落指纹 | **可重置为空库**（首章 `--commit` 重建）。代价：新 ch001 章检前会报 `fingerprint-arrears`(hard)，按既定流程「先 --commit 补齐再过章检」即可 |
| `追踪/待审台账.md`（14K） | 决策日志（保留价值高：世次裁决、吃饭了没裁决、overcompressed 改判都在里面）；但表内 1/2/3 章的「顺延@ch004 批」行随旧稿作废 | **保留**，只在重写后把 1/2/3 章的顺延行标注「旧稿作废」 |
| `追踪/豁免台账.md`（3.3K） | ch002/ch003 的豁免记录（章节已删） | **保留**作审计痕迹 |

另：`追踪/读者信号.md` 仍是旧 10 列格式（缺「验收检查点」节），新模板 14 列 —— 属另一次升级，未动。

## 五、下一步

状态机已回到 ch000，`check` 绿。进 guyin-write 重写第 1 章：细纲 → 执笔 → 审读 → tracking-commit 落盘。
